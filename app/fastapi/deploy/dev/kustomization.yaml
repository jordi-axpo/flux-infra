apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: default
resources:
  - ../base
configMapGenerator:
  - name: env-example # it will replace the configmap that it founds with this name and add a hash
    behavior: merge
    literals:
      - ENV=dev
commonLabels:
  environment: dev
images: 
  - name: h2axsa4sandbox4acr.azurecr.io/hello-version-fastapi-dev:25 # {"$imagepolicy": "default:fastapi"}