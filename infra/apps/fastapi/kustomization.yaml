apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: fastapi
  namespace: default
spec:
  interval: 15m
  path: "./infra/apps/fastapi/deploy"
  prune: true
  sourceRef:
    kind: GitRepository
    name: fastapi