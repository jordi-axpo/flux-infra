apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: fastapi
  namespace: default
spec:
  interval: 1m0s
  path: "./infra/apps/base/fastapi/deploy"
  prune: true
  sourceRef:
    kind: GitRepository
    name: fastapi